import 'package:core/domain.dart';

/// Навигационный контракт фичи «Заявители/кураторы». Реализация (знающая
/// go_router-пути приложения) живёт в корневом навигационном слое и инъектится в
/// модуль — так модуль не зависит ни от go_router, ни от роутов приложения.
///
/// Экраны редактирования возвращают сохранённую сущность через
/// `Navigator.pop(result)` (framework-навигация, без знания о роутах
/// приложения), поэтому дополнительных переходов контракт пока не объявляет —
/// он остаётся маркером фичи в едином ряду с animals/auth router-сервисами.
abstract interface class ApplicantsRouterService implements RouterService {}
