/// Каталог компонентов дизайн-системы (галерея) — отдельная точка входа.
///
/// Держится ОТДЕЛЬНО от `ui_kit.dart`, чтобы страницы-экспонаты и их зависимости
/// не попадали в прод-бандл приложений, которым нужны только токены/компоненты.
/// Импортируют это только dev-хосты: standalone example-апп и debug-экран
/// (под `kDebugMode`). Сам каталог не зависит от widgetbook — мост в конкретный
/// инструмент строит хост.
library;

export 'gallery/gallery.dart';
