name: base
description: Cross-cutting core for ACITS — Result/Failure, Dio client + interceptors, AppTask pipeline, platform-service ports.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  equatable: ^2.0.8
  flutter_bloc: ^9.1.1
  intl: ^0.20.2
  package_info_plus: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5
