/// Контракт маппера DTO → доменная сущность. Отдельный класс (не метод на DTO),
/// чтобы сборка сущности из нескольких DTO оставалась чистой. Реализации живут
/// в feature.data/mapper и импортируют DTO — домен про них не знает.
abstract interface class Transformable<T> {
  T toEntity();
}
