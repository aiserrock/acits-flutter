name: animals
description: Animals feature module (reference slice) — list / detail / edit on the new stack. data/domain/ui, DTO-contained via acits_api ports.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  dio: ^5.9.2
  easy_localization: ^3.0.8
  flutter_slidable: ^4.0.3
  base:
    path: ../base
  l10n:
    path: ../l10n
  acits_domain:
    path: ../../packages/acits_domain
  acits_api:
    path: ../../packages/acits_api
  ui_kit:
    path: ../ui_kit
  navigation:
    path: ../navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.5
