org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G
android.useAndroidX=true
android.nonTransitiveRClass=true
# Built-in Kotlin включён (приложение не применяет kotlin-android, Kotlin —
# через kotlin{}-блок в app/build.gradle). newDsl — новый AGP DSL под built-in.
android.builtInKotlin=true
android.newDsl=true
