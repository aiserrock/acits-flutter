/// API layer: stable ports (`abstract <Feature>ApiPort`) + our freezed DTOs.
///
/// The generator (`swagger_parser`) lives under `adapters/` and is the only
/// place generated code exists. Swapping generators = rewrite one adapter.
library;
