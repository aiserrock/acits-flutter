name: acits_domain
description: Shared domain layer for ACITS — entities, repository interfaces, Transformable mapper contract, RouterService base. DTO-free.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.8
  base:
    path: ../../modules/base

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
